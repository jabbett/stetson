var $commentList = $(".comment-list"),
    $form = $(".comments form");

$commentList.append("<%= escape_javascript(render partial: 'comments/comment', locals: { comment: @comment }) %>");
$form[0].reset();