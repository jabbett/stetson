<% if page.parent.present? %>
<div class="stetson-parent">
  <div class="container">
    <%= link_to "#{page.parent.title}".html_safe, page.parent %>
  </div>
</div>
<% end %>
<div class="stetson-page-meta">
  <div class="container">
    <div class="meta">
      <h1><%= link_to page.title, page_path(page) %></h1>
    </div>
    <% if page.paper_trail.live? %>
      <div class="actions">
        <%= icon('loader', classes: 'spin hide') %>
        <%= link_to icon('paperclip'), 'javascript:void(0);', class: 'btn-attach' %>
        <%= link_to icon('clock'), page_versions_path(page) %>
        <%= link_to icon('trash-2'), page_path(page), method: :delete, data: { confirm: "Really delete page '#{page.title}'?" } %>
        <%= link_to icon('file-plus'), new_page_path(parent_id: page.id) %>
        <%= link_to icon('edit-2'), edit_page_path(page) %>
      </div>
    <% end %>
  </div>
</div>